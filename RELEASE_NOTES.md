# 清华教参下载器 1.0 发布说明

**发布日期**：2026 年 9 月
**适用平台**：Windows 10 64 位及以上
**安装包**：`清华教参下载器-1.0-安装包.exe`（约 133 MB，无需管理员权限）

---

## 这是什么

把清华教参平台上的电子教材整本下载成清晰的 PDF。

这个版本是一次**彻底的推倒重写**：上游的 Python 脚本被重做成一个带图形界面的
Windows 桌面应用。你不再需要装 Python、不需要敲命令行、不需要记参数。

装好、打开、登录一次，之后就是"粘链接 → 等下载 → 去书库拿 PDF"。

---

## 这个版本有什么

### 图形界面，不用碰命令行

左侧导航 + 右侧工作区，四个页面：

| 页面 | 做什么 |
| --- | --- |
| **下载队列** | 粘书籍链接，一行一条，可一次粘很多本；点"开始下载"后逐本下完 |
| **书库** | 已下载的书，带封面、页数、体积；点封面开 PDF，点文件夹定位目录 |
| **设置** | Token、保存位置、进程数、PDF 质量、是否保留临时图片 |
| **使用说明** | 怎么抓 Token、常见问题、版权提示，都在应用里 |

### 登录：两种方式，推荐第一种

- **统一身份认证**——在应用内嵌的浏览器里输账号密码、完成双因子认证，
  Token **自动截获**，你不用复制任何东西。
- **直接填 Token**——不想在应用里输密码就用这个，从浏览器 F12 抓一串粘进来。

内嵌浏览器用的是**持久化配置**，所以勾了"信任此设备"之后，下次 Token 过期
可以真的自动续期，而不是每次都重走一遍双因子。

### Token 过期不用你操心

清华教参的 Token 实测**只活 63 分钟**。程序会：

- 在侧边栏底部显示**当前账号**（姓名 + 学号）和**剩余时间**，悬停能看到完整到期时刻；
- 剩余时间不足时提示"建议尽快重新登录"；
- 下载中途 Token 失效时，**自动回到登录页重新获取，再自动接上刚才失败的那本书**，
  不用你手动点重试。

### 批量排队 + 断点续传

一次粘多条链接，程序一本一本地下。

- 中途可以随时**停止**——已经下好的页面会保留，下次接着下；
- 失败的书标明原因，点**"重试失败"**重来，**已成功下载的页面不会重复下载**，
  所以重试很便宜；
- 关掉程序再打开也是这样，不会从头再来。

### 书库

下载完自动入库，带封面、书名、作者、页数和体积，可搜索、可直接打开 PDF、
可一键定位到文件所在目录。

### 一处小细节

侧边栏那行倒计时**每 30 秒自己走字**。只在切页面时才刷的话，程序静置半小时后
那行字就是错的。

---

## 界面

- 全部**自绘**：无图片资源、无图标字体、无 emoji，动效可完全关闭。
- **无边框窗口 + 自绘标题栏**，但**拖动和缩放用系统原生行为**（包括拖到屏幕边缘
  自动吸附），手感和资源管理器一致，不是"自己搬窗口"那种廉价实现。
- 设计令牌集中在 `xk_app/app/gui/theme.py`，改设计只改那一处。

---

## 安装

1. 下载 `清华教参下载器-1.0-安装包.exe`。
2. 双击，一路"下一步"。**不需要管理员权限**，默认装在
   `%LOCALAPPDATA%\Programs\TsinghuaBookCrawler`。
3. 从开始菜单或桌面快捷方式启动。

> **Windows 弹出"已保护你的电脑"**：安装包没有购买代码签名证书，SmartScreen 会拦一下。
> 点"更多信息" → "仍要运行"即可。

### 系统要求

- Windows 10 64 位或更高
- 约 500 MB 磁盘空间（含内嵌浏览器）
- 能访问 `*.tsinghua.edu.cn` 和 `*.lib.tsinghua.edu.cn` 的网络

---

## 常见问题

**提示 Token 不正确 / 已过期**
Token 和登录会话绑定，会过期。统一身份认证模式会自己续；手动填 Token 的重新抓一个。

**下载到一半失败了**
在"下载队列"里点"重试失败"。已成功下载的页面不会重复下载。

**某本书一直失败**
先确认这本书在你的账号权限范围内，并且链接是 `bookDetail` 详情页链接。
也可以在"设置"里把进程数调到 2 再试——有些网络环境并发一高就会被拒。

**PDF 太大 / 太糊**
在"设置"里调"PDF 质量"。10 最高最清晰，往小调会明显减小体积。

**想省磁盘**
在"设置"里取消勾选"保留临时图片"，生成 PDF 后自动删掉图片。

**登录页的内嵌浏览器打不开**
少见，通常是系统缺运行库。程序会明确告诉你，并让你改用"直接填 Token"。

---

## 数据与卸载

配置、书库记录和登录会话存在 `%LOCALAPPDATA%\TsinghuaBookCrawler`。

**卸载不会删这个目录，也不会删你下载的 PDF。** 想彻底清干净（包括登录状态），
卸载后手动删掉它即可。

---

## 版权

下载得到的电子书**版权归原作者与出版方所有，仅供个人学习**。
请不要传播，尤其是不要传播给校外未授权者；也请避免批量下载整库书籍。
本程序不提供任何绕过认证或授权的能力。

一切滥用本程序导致的不良后果，作者概不负责。
