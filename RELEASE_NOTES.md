# 清华教参下载器 2.1.1

**发布日期**：2026 年 9 月
**适用平台**：Windows 10 64 位及以上
**安装包**：`TsinghuaBookCrawler-2.1.1-Setup.exe`（约 133 MB，无需管理员权限）

---

## 本版修复

修掉 2.1 里应用内更新的一个致命问题：**点「下载并安装」后没有任何反应**。

原因是启动安装包的命令行拼错了。Windows 上把命令拆成参数列表传给子进程时，
系统会再拼接一次命令行并额外加一层引号，导致 `cmd.exe` 无法解析；
而这一步不会报错，安装包就这样被静默丢弃。现改为把命令原样交给命令行解释器。

同时修正了「装完自动重新打开程序」的判定：安装程序会保留文件原有的时间戳，
原先按修改时间判断安装是否完成，重装同一版本时会误判为失败。

如果你装的是 2.1，程序会在下次启动时提示更新到 2.1.1，点一下即可完成。
也可以直接下载下面的安装包覆盖安装，配置与已下载的书都不会丢。

## 安装

1. 下载 `TsinghuaBookCrawler-2.1.1-Setup.exe`
2. 运行安装程序，按提示完成安装（默认路径 `%LOCALAPPDATA%\Programs\TsinghuaBookCrawler`）
3. 从开始菜单或桌面快捷方式启动

> 安装包未购买代码签名证书，Windows SmartScreen 可能提示"已保护你的电脑"。
> 选择"更多信息"→"仍要运行"即可。

## 系统要求

- Windows 10 64 位及以上
- 约 500 MB 磁盘空间
- 可访问 `*.tsinghua.edu.cn` 与 `*.lib.tsinghua.edu.cn`

## 数据与卸载

配置、书库记录与登录会话存放于 `%LOCALAPPDATA%\TsinghuaBookCrawler`。
卸载不会删除该目录，也不会删除已下载的 PDF。

## 版权

下载所得的电子书版权归原作者与出版方所有，仅供个人学习使用。
请勿传播，亦请避免批量下载。本程序不提供任何绕过认证或授权的能力。
因滥用本程序产生的一切后果，作者不承担责任。

---

完整使用说明见 [README](README.md)。
